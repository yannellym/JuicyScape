import styled from "styled-components";

const Section = styled.section`
    position: relative;
    min-height: 100vh;
    overflow: hidden;
`

const About = () => {
    return (
        <Section>
            About
        </Section>
    )
}

export default About;